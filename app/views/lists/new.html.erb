<h1>Add a New List</h1>

<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file, label: "Upload an image for the list (optional)" %>
  <%= f.submit %>
<% end %>

<p><%= link_to "Back to All Lists", lists_path %></p>
